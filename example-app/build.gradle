plugins {
    id 'application'
}

dependencies {
    implementation project(':annotation-processor')      // for annotation types available to compile
    annotationProcessor project(':annotation-processor') // run processors
}

application {
    mainClass = 'com.example.Main'
}